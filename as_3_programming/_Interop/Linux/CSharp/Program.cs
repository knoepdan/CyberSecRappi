using System;

namespace CSharp
{
    class Program
    {
        // to build: "dotnet build CSharp.csproj"
        // to run (after building): "dotnet run CSharp.dll"   (here dll is in same directory)
        // to debug: ?? didnt work out of the box with Linux and VS Code
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");
        }
    }
}
